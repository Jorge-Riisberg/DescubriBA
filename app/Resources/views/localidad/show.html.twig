{% extends 'base.html.twig' %}

{% block body %}
    <h1>Localidad <strong>{{ localidad.nombre }}</strong></h1><br>
    <div>
        <table class="table table-hover">
            <tbody>
                <tr>
                    <th>ID</th>
                    <th>NOMBRE</th>
                    
                </tr>
                <tr>
                    <td>{{ localidad.id }}</td>
                    <td>{{ localidad.nombre }}</td>
                </tr>
            </tbody>
        </table>
        <a href="{{ path('localidad_index') }}" class="btn btn-primary" role="button">Volver</a>
        <a href="{{ path('localidad_edit', { 'id': localidad.id }) }}" class="btn btn-warning" role="button">Modificar</a>
        {{ form_start(delete_form) }}
            <br><input type="submit" value="Eliminar" class="btn btn-danger" role="button">
        {{ form_end(delete_form) }}
    </div>

{% endblock %}
