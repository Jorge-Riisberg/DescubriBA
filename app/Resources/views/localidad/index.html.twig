{% extends 'base.html.twig' %}

{% block body %}
    <h1>Listado de <strong>Localidades</strong></h1><br>
    <div>
        <table id="tabla_buscar" class="table table-hover">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>NOMBRE</th>
                    <th>ACCIONES</th>
                </tr>
            </thead>
            <tbody>
            {% for localidad in localidads %}
                <tr>
                    <td>{{ localidad.id }}</td>
                    <td>{{ localidad.nombre }}</td>
                    <td>
                        <a href="{{ path('localidad_show', { 'id': localidad.id }) }}" class="btn btn-info btn-xs" role="button">Visualizar</a>
                        <a href="{{ path('localidad_edit', { 'id': localidad.id }) }}" class="btn btn-warning btn-xs" role="button">Modificar</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    <a href="{{ path('localidad_new') }}" class="btn btn-success btn-md" role="button">Nueva localidad</a>
{% endblock %}
